---
import { getCollection } from "astro:content";
import GroupedPostListPage from "@components/GroupedPostListPage.astro";
import { archiveGroupConfig, getGroupedFirstPageProps, getGroupedStaticPaths } from "@/grouped-posts.ts";

export async function getStaticPaths() {
  return getGroupedStaticPaths(await getCollection("blog"), archiveGroupConfig);
}

const pageProps = await getGroupedFirstPageProps(Astro.props);
---

<GroupedPostListPage {...pageProps} />
